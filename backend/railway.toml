# Railway Backend Configuration

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python docker_init.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[healthcheck]
path = "/health"
timeout = 300
interval = 30
