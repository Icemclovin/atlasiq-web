{
  "phases": {
    "setup": {
      "nixPkgs": ["python311"]
    },
    "install": {
      "cmds": ["pip install -r requirements.txt"]
    }
  },
  "start": {
    "cmd": "python docker_init.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
